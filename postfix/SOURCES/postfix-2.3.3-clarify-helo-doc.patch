diff --git a/html/postconf.5.html b/html/postconf.5.html
index 3b2d154..5803769 100644
--- a/html/postconf.5.html
+++ b/html/postconf.5.html
@@ -9684,8 +9684,8 @@ feature is available in Postfix 2.1 and later.  </dd>
 
 <dt><b><a name="reject_invalid_helo_hostname">reject_invalid_helo_hostname</a></b> (with Postfix &lt; 2.3: reject_invalid_hostname)</dt>
 
-<dd>Reject the request when the HELO or EHLO hostname syntax is
-invalid. <br> The <a href="postconf.5.html#invalid_hostname_reject_code">invalid_hostname_reject_code</a> specifies the response
+<dd>Reject the request when the HELO or EHLO command specifies a malformed
+hostname. <br> The <a href="postconf.5.html#invalid_hostname_reject_code">invalid_hostname_reject_code</a> specifies the response
 code to rejected requests (default: 501).</dd>
 
 <dt><b><a name="reject_non_fqdn_helo_hostname">reject_non_fqdn_helo_hostname</a></b> (with Postfix &lt; 2.3: reject_non_fqdn_hostname)</dt>
diff --git a/man/man5/postconf.5 b/man/man5/postconf.5
index c37d800..4db642b 100644
--- a/man/man5/postconf.5
+++ b/man/man5/postconf.5
@@ -5755,8 +5755,8 @@ Note: a result of "OK" is not allowed for safety reasons. Instead,
 use DUNNO in order to exclude specific hosts from blacklists.  This
 feature is available in Postfix 2.1 and later.
 .IP "\fBreject_invalid_helo_hostname\fR (with Postfix < 2.3: reject_invalid_hostname)"
-Reject the request when the HELO or EHLO hostname syntax is
-invalid.
+Reject the request when the HELO or EHLO command specifies a malformed
+hostname.
 .br
 The invalid_hostname_reject_code specifies the response
 code to rejected requests (default: 501).
diff --git a/proto/postconf.proto b/proto/postconf.proto
index f031b6f..5bec009 100644
--- a/proto/postconf.proto
+++ b/proto/postconf.proto
@@ -5025,8 +5025,8 @@ feature is available in Postfix 2.1 and later.  </dd>
 
 <dt><b><a name="reject_invalid_helo_hostname">reject_invalid_helo_hostname</a></b> (with Postfix &lt; 2.3: reject_invalid_hostname)</dt>
 
-<dd>Reject the request when the HELO or EHLO hostname syntax is
-invalid. <br> The invalid_hostname_reject_code specifies the response
+<dd>Reject the request when the HELO or EHLO command specifies a malformed
+hostname. <br> The invalid_hostname_reject_code specifies the response
 code to rejected requests (default: 501).</dd>
 
 <dt><b><a name="reject_non_fqdn_helo_hostname">reject_non_fqdn_helo_hostname</a></b> (with Postfix &lt; 2.3: reject_non_fqdn_hostname)</dt>
